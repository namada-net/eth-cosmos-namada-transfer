# WETH → Cosmos Hub → Namada PoC

## Setup
```bash
npm install
npm run dev
```
Open browser at http://localhost:5173

Use MetaMask (Ethereum mainnet) and Keplr (cosmoshub-4). Enter a Namada transparent address and click step1 then step2.
